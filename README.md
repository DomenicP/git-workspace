# git-workspace

Manage multiple Git repositories as a workspace.

Run Git commands (fetch, status, checkout, ...) across a set of multiple repositoriesFor full usage instructions, run `git workspace help`.

## Installation

### Go

```sh
go install github.com/DomenicP/git-workspace@latest
```
